#include "main.h"
/**
 * _islower - Check for lowercase characters
 * @c: character to check type letter
 *Return: 1 if character is lowercase, 0 if otherwise
 */

int _islower(int c)
{
if (c >= 'a' && c <= 'z')
putchar("\n");
return (1);

else
putchar("\n");
return (0);
}
